export type Language = {
  id: number;
  name: string;
  code: string;
};

export type WordPair = {
    id: number;
    word1: string;
    language1: string;
    word2: string;
    language2: string;
};
